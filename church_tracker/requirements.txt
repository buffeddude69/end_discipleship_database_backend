# Core -- needed everywhere, including local development.
Django>=5.0,<6.1
djangorestframework>=3.15
psycopg2-binary>=2.9
django-cors-headers>=4.4
Pillow>=10.0

# Production-only -- used for deployment (Render + Supabase). Django is set
# up in config/settings.py to run fine locally without these installed at
# all; they're only imported/activated when their related environment
# variables are actually set. You still need them installed wherever you
# deploy (Render installs from this file automatically), just not on your
# local machine unless you want to test the production storage/DB path.
django-storages>=1.14
boto3>=1.34
gunicorn>=22.0
whitenoise>=6.7
dj-database-url>=2.2
